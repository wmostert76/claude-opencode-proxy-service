{
  "name": "claude-opencode-proxy-service",
  "version": "0.4.0",
  "description": "Run Claude Code through OpenCode Go using a local Anthropic-to-OpenAI-compatible proxy.",
  "type": "module",
  "bin": {
    "claude-opencode": "bin/claude-opencode"
  },
  "scripts": {
    "check": "node --check proxy/anthropic2openai-proxy.mjs && bash -n bin/claude-opencode scripts/install.sh scripts/uninstall.sh",
    "models": "bin/claude-opencode --model"
  },
  "engines": {
    "node": ">=20"
  }
}
